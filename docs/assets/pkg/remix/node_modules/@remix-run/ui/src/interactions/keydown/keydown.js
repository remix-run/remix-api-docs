import { createMixin, on } from "/assets/pkg/remix/node_modules/@remix-run/ui/src/index.js";
export const onKeyDown = createMixin(() => (key, handler) => on("keydown", (event) => {
	if (event.key === key) {
		event.preventDefault();
		handler(event);
	}
}));